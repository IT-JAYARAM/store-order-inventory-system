@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1>Dashboard</h1><p class="text-muted mb-0">Products, customers, orders and low-stock monitoring.</p></div><a class="btn btn-primary" href="{{ route('orders.create') }}">+ New Order</a></div>
<div class="row g-3 mb-4">@foreach([['Products',$productCount],['Customers',$customerCount],['Orders',$orderCount]] as [$label,$value])<div class="col-md-4"><div class="card shadow-sm"><div class="card-body"><div class="text-muted">{{ $label }}</div><div class="display-6 fw-bold">{{ $value }}</div></div></div></div>@endforeach</div>
<div class="card shadow-sm mb-4"><div class="card-header fw-bold">Low Stock (≤ {{ config('store.low_stock_threshold') }})</div><div class="card-body p-0"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Product</th><th>Code</th><th>Stock</th><th>Price</th></tr></thead><tbody>@forelse($lowStockProducts as $p)<tr><td>{{ $p->name }}</td><td>{{ $p->code }}</td><td><span class="badge text-bg-danger">{{ $p->stock_on_hand }}</span></td><td>₹{{ number_format($p->price,2) }}</td></tr>@empty<tr><td colspan="4" class="text-center py-3">No low-stock products.</td></tr>@endforelse</tbody></table></div></div></div>
<div class="card shadow-sm"><div class="card-header fw-bold">Recent Orders</div><div class="card-body p-0"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>ID</th><th>Customer</th><th>Total</th><th>Date</th></tr></thead><tbody>@forelse($recentOrders as $o)<tr><td><a href="{{ route('orders.show',$o) }}">#{{ $o->id }}</a></td><td>{{ $o->customer->name }}<div class="small text-muted">{{ $o->customer->email }}</div></td><td>₹{{ number_format($o->grand_total,2) }}</td><td>{{ $o->created_at->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="4" class="text-center py-3">No orders yet.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection

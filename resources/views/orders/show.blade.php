@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1>Order #{{ $order->id }}</h1><a href="{{ route('dashboard') }}">Back to dashboard</a></div><div class="row g-3 mb-3"><div class="col-md-6"><div class="card shadow-sm"><div class="card-body"><h5>Customer</h5><div>{{ $order->customer->name }}</div><div class="text-muted">{{ $order->customer->email }}</div></div></div></div><div class="col-md-6"><div class="card shadow-sm"><div class="card-body"><h5>Totals</h5><div>Subtotal: ₹{{ number_format($order->subtotal,2) }}</div><div>Tax: ₹{{ number_format($order->tax,2) }}</div><div class="fw-bold">Grand Total: ₹{{ number_format($order->grand_total,2) }}</div></div></div></div></div><div class="card shadow-sm"><div class="card-body p-0"><table class="table mb-0"><thead><tr><th>Product</th><th>Qty</th><th>Unit Price</th><th>Tax</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->quantity }}</td><td>₹{{ number_format($item->unit_price,2) }}</td><td>{{ number_format($item->tax_percentage,2) }}% / ₹{{ number_format($item->line_tax,2) }}</td><td>₹{{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody></table></div></div>
@endsection
